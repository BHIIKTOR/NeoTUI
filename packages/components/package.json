{
  "name": "@neotui/components",
  "version": "0.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "dependencies": {
    "@neotui/core": "workspace:*"
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun --external @neotui/core",
    "typecheck": "bunx tsc -p tsconfig.json --noEmit",
    "lint": "bunx @biomejs/biome check src package.json tsconfig.json"
  }
}
