{
  "name": "@neotui/test-utils",
  "version": "0.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "dependencies": {
    "@neotui/components": "workspace:*",
    "@neotui/core": "workspace:*",
    "@neotui/fixtures": "workspace:*"
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun",
    "typecheck": "bunx tsc -p tsconfig.json --noEmit",
    "lint": "bunx @biomejs/biome check src tests package.json tsconfig.json"
  }
}
